This checked-in transcript is intentionally pending regeneration after the
vllm-vcr migration. Run `./run.sh --quick --teardown` to generate a fresh
transcript and evidence from the VCR-backed topology.

The previous transcript described the retired mock-provider backend and must
not be used as migration evidence.
