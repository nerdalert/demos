apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: vcr-inference-backend
  namespace: grid-system
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: vllm-vcr
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              grid.praxis-proxy.io/backend-access: "provider-gateway"
      ports:
        - protocol: TCP
          port: 8000
